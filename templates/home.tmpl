{{define "title"}}Posts{{end}}

{{define "body"}}
<h1>Posts</h1>

<fieldset>
    <legend><a href="{{.Paths.NewPost}}">[+] Create new post</a></legend>
    <ul>
        {{range .Posts}}
        <li>{{.CreatedAt.Format "2006-01-02"}} — <a href="{{$.Paths.EditPost}}{{.Id}}">{{.Title}}</a></li>
        {{end}}
    </ul>
</fieldset>

<style>
    fieldset a {
        text-decoration: none;
    }

    fieldset ul {
        padding: 0;
        list-style-type: none;
        font-size: 1.1rem;
    }
</style>

<p><a href="{{.Paths.Preview}}">Preview output</a></p>
{{end}}
